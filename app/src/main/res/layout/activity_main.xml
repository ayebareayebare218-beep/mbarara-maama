<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="#FAFAFA">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Mbarara Mama-Check AI"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#6200EA"
            android:gravity="center"
            android:layout_marginTop="30dp"
            android:layout_marginBottom="20dp"/>

        <!-- 1. Age -->
        <EditText
            android:id="@+id/editAge"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Maternal Age (e.g. 25)"
            android:inputType="numberDecimal"
            android:layout_marginBottom="10dp"/>

        <!-- 2. Systolic BP -->
        <EditText
            android:id="@+id/editSystolicBP"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Systolic BP (Top number, e.g. 120)"
            android:inputType="numberDecimal"
            android:layout_marginBottom="10dp"/>

        <!-- 3. Diastolic BP -->
        <EditText
            android:id="@+id/editDiastolicBP"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Diastolic BP (Bottom number, e.g. 80)"
            android:inputType="numberDecimal"
            android:layout_marginBottom="10dp"/>

        <!-- 4. Blood Sugar -->
        <EditText
            android:id="@+id/editBloodSugar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Blood Sugar (mmol/L, e.g. 7.5)"
            android:inputType="numberDecimal"
            android:layout_marginBottom="10dp"/>

        <!-- 5. Body Temperature -->
        <EditText
            android:id="@+id/editBodyTemp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Body Temp (°C, e.g. 37.0)"
            android:inputType="numberDecimal"
            android:layout_marginBottom="10dp"/>

        <!-- 6. Heart Rate -->
        <EditText
            android:id="@+id/editHeartRate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Heart Rate (bpm, e.g. 80)"
            android:inputType="numberDecimal"
            android:layout_marginBottom="20dp"/>

        <Button
            android:id="@+id/btnAssess"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="START ASSESSMENT"
            android:backgroundTint="#6200EA"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:layout_marginBottom="20dp"/>

        <!-- Result Card -->
        <LinearLayout
            android:id="@+id/resultLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:visibility="gone"
            android:background="#E0E0E0">

            <TextView
                android:id="@+id/textResultTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#000000"
                android:gravity="center"/>

            <TextView
                android:id="@+id/textResultAction"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textColor="#000000"
                android:gravity="center"
                android:layout_marginTop="10dp"/>
        </LinearLayout>

    </LinearLayout>
</ScrollView>